services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: life
    ports:
      - "5432:5432"
    volumes:
      - life_pg_data:/var/lib/postgresql/data

volumes:
  life_pg_data:
